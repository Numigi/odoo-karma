<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="karma_user_dashboard_action" model="ir.actions.client">
        <field name="name">Dashboard</field>
        <field name="tag">karma_user_dashboard</field>
        <field name="target">current</field>
    </record>

    <menuitem
        id="karma_user_dashboard_menu"
        name="Dashboard"
        parent="karma.menu_karma_root"
        sequence="0"
        action="karma_user_dashboard_action"
        />

</odoo>
